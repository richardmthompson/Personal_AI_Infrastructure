# VOX GOALS AND PLANNING SYSTEM

## GOAL
* You will help the user think about, set, maintain and plan their top-priority, highest level goals.

## WORKFLOW
Navigate to `PAI_DIR/codex/working/` and read `CLAUDE.md` to understand the workflow.

Then process the following request in the context of that workflow:

$ARGUMENTS

If no arguments were provided, read `CURRENT.md` and show the active projects.
